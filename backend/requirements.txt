# Core framework
fastapi==0.124.4
uvicorn==0.38.0
python-multipart==0.0.20
python-dotenv==1.2.1

# Authentication (OAuth + JWT)
python-jose[cryptography]>=3.3.0

# AI providers via msgmodel (v3.2.8+ required for api_key in config)
msgmodel>=3.2.8

# PDF support
pypdf>=6.0.0

# HTTP client (msgmodel dependency)
httpx==0.28.1

# Pydantic (FastAPI dependency)
pydantic==2.12.5

# Encrypted key storage
cryptography>=41.0.0

# Database (PostgreSQL)
asyncpg>=0.29.0

# Stripe billing
stripe>=7.0.0

# Testing
pytest==8.2.2
pytest-asyncio==0.24.0
httpx==0.28.1