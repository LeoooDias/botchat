# Backend environment example (privacy-first)

# ===========================================
# AUTHENTICATION (OAuth + JWT)
# ===========================================

# Set to "true" to require OAuth authentication
# Default: "false" for local development (API key auth still works)
REQUIRE_AUTH=false

# JWT Secret - REQUIRED for production
# Generate with: openssl rand -hex 32
JWT_SECRET=replace-with-random-secret-for-jwt

# GitHub OAuth (https://github.com/settings/developers)
# Create a new OAuth App:
# - Homepage URL: https://yourdomain.com
# - Authorization callback URL: https://yourdomain.com/auth/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Google OAuth (https://console.cloud.google.com/apis/credentials)
# Create OAuth 2.0 Client ID:
# - Authorized JavaScript origins: https://yourdomain.com
# - Authorized redirect URIs: https://yourdomain.com/auth/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ===========================================
# LEGACY API KEY AUTH (for self-hosted/dev)
# ===========================================

# Generate a strong random secret, e.g.:
#   openssl rand -hex 32
API_KEY=replace-with-random-secret

# Set to "false" to disable API key requirement entirely
REQUIRE_API_KEY=true

# ===========================================
# CORS & LOGGING
# ===========================================

# Comma-separated, no '*'
CORS_ORIGINS=http://localhost:5173

# Provider keys (keep private)
OPENAI_API_KEY=replace-with-openai-key
GOOGLE_API_KEY=replace-with-gemini-key
ANTHROPIC_API_KEY=replace-with-anthropic-key

# Optional tuning
LOG_LEVEL=WARNING
ALLOW_DEBUG_LOGS=false
DISABLE_ACCESS_LOGS=true
RUN_TTL_SECONDS=60
