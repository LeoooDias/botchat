# Frontend environment (for local development)
# Copy to .env.local: cp .env.example .env.local

VITE_API_BASE=http://localhost:8000

# Must match the API_KEY in backend/.env
# This is a shared secret for frontend→backend authentication
VITE_API_KEY=replace-with-same-value-as-backend-API_KEY